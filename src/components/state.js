import React, { Component } from "react";

export default class State extends Component {
  render() {
    return (
      <div className="page-container">
        <h1>State</h1>
        <div>
          Your goal on this page is to implement a constructor and get a piece
          of state to show on the page!
          <p>
            (bonus if you use some component lifecycle methods to show what
            order your process is running)
          </p>
        </div>
      </div>
    );
  }
}
